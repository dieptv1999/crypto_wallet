type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String
  username: String
  email: String!
  password: String!
  createdAt: AWSTimestamp
  updatedAt: AWSTimestamp
  status: Int
}
